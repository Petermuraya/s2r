package com.s2r.smarts2r.security;

/** Disabled duplicate SecurityConfig. Keep `com.s2r.smarts2r.config.SecurityConfig` as the active config. */
@Deprecated
public class SecurityConfig {
    // intentionally empty - duplicate disabled
}
